[theme]
primaryColor = "#0d9488"
backgroundColor = "#f4f7f6"
secondaryBackgroundColor = "#ffffff"
textColor = "#0f172a"
font = "sans serif"

[server]
headless = true
# WhatsApp / long clips: avoid Streamlit rejecting uploads (red row) on Spaces (value is MB).
maxUploadSize = 500
